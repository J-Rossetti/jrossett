apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wilapp-ingress
  namespace: dev
spec:
  rules:
  - host:
    http:
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: wilapp-service
              port:
                number: 80
